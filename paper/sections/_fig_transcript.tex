%% This figure was withdrawn from the paper on 2026-09-11; the body no longer \inputs it.
%%
%% Why: after the body was compressed, two of the three things the figure showed each
%% take a single clause of prose, and the one thing only it showed -- that the model
%% copies the tool's point verbatim -- is now reported as 384 of 693 turns (55%),
%% computed by verbatim_copy_rate() in scripts/tool_regret.py, which is far stronger
%% than a single episode.
%%
%% The file is kept as an empty shell so that scripts/check_crossrefs.py no longer
%% reports fig:transcript as an orphan label. To bring the figure back, run
%% scripts/make_fig_transcript.py, which now writes to results/ rather than here.
